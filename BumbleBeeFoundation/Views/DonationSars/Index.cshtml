@model IEnumerable<BumbleBeeFoundation.Models.DonationSar>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GeneratedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sarsdocument)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Donation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GeneratedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sarsdocument)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Donation.DonationId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Sarsid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Sarsid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Sarsid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
